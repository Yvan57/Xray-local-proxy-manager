# ⚡ Xray Local Proxy Manager

**Полнофункциональный менеджер локальных прокси через Xray с реальным мониторингом активности и автосохранением конфигурации.**

![Python](https://img.shields.io/badge/Python-3.7+-blue)
![License](https://img.shields.io/badge/License-MIT-green)
![Status](https://img.shields.io/badge/Status-Stable-brightgreen)
![Version](https://img.shields.io/badge/Version-3.3-blue)

---

## 🎯 Что это?

**Xray Local Proxy Manager** — это удобный инструмент для управления несколькими локальными SOCKS5 прокси одновременно. Позволяет:

✅ **Управлять несколькими портами** с разными профилями (VLESS, VMess, Trojan, Shadowsocks)
✅ **Автосохранение конфигурации** — при перезагрузке все портов загружаются автоматически
✅ **Проверка IP** через два API с поддержкой SOCKS5 прокси
✅ **Реальный мониторинг активности** — видеть активные соединения, IP адреса, трафик в реальном времени
✅ **Интерактивное меню** с навигацией стрелками и выбором из списков
✅ **Два режима работы**: Unified (экономия ресурсов) и Separate (независимое управление)
✅ **Красивый интерфейс** с цветным выводом и статусом портов

---

## 📋 Требования

- **Windows 7+** или **Linux/macOS**
- **Python 3.7+**
- **Xray Core** (v26.1.23 и выше) — [скачать](https://github.com/XTLS/Xray-core/releases)

> 🔧 **Протестировано на:** Xray Core v26.1.23+

---

## 🚀 Как установить и запустить?

### Шаг 1️⃣: Проверьте Python

1. **Откройте командную строку (CMD)**:
   - Нажмите `Win + R`
   - Введите `cmd` и нажмите **Enter**

2. **Проверьте версию Python**:
   ```
   python --version
   ```
   
   ✅ Если видите `Python 3.x.x` — переходите к шагу 2
   ❌ Если ошибка — установите Python ниже

### Если Python не установлен 📥

1. **Скачайте Python**:
   - Перейдите на: https://www.python.org/downloads/windows/
   - Нажмите **Download Python 3.x.x**

2. **Установите Python**:
   - Запустите установщик
   - ⚠️ **ВАЖНО**: Поставьте галочку **"Add Python to PATH"**
   - Следуйте инструкциям

3. **Проверьте установку**:
   ```
   python --version
   ```

---

### Шаг 2️⃣: Скачайте Xray Core

1. **Скачайте Xray**:
   - Перейдите на: https://github.com/XTLS/Xray-core/releases
   - Нажмите на **[последнюю версию](https://github.com/XTLS/Xray-core/releases/latest)**
   - Скачайте `Xray-windows-64.zip` (для Windows) или подходящий для вашей ОС

2. **Распакуйте архив**:
   - Найдите скачанный ZIP
   - Нажмите правой кнопкой → **"Извлечь всё..."**
   - Распакуйте в удобное место (например, `C:\xray\`)

3. **Проверьте**: в папке должен быть файл `xray.exe` (для Windows)

---

### Шаг 3️⃣: Скачайте файл утилиты

1. **Скачайте последнюю версию**:
   - Перейдите на [страницу последнего релиза](https://github.com/Yvan57/Xray-local-proxy-manager/releases/latest)
   - Найдите файл `xray_proxy_manager.py` в разделе **"Assets"**
   - Нажмите на него для скачивания

2. **Поместите файл в удобное место**:
   - Создайте папку для утилиты (например, `C:\XrayManager\`)
   - Поместите скачанный `xray_proxy_manager.py` в эту папку

---

### Шаг 4️⃣: Установите зависимости Python

1. **Скачайте файл зависимостей**:
   - Перейдите на [страницу последнего релиза](https://github.com/Yvan57/Xray-local-proxy-manager/releases/latest)
   - Найдите файл `requirements.txt` в разделе **"Assets"**
   - Нажмите на него для скачивания
   - Поместите файл в ту же папку, где находится `xray_proxy_manager.py`

2. **Откройте командную строку в папке проекта**:
   - Перейдите в папку с файлами
   - В адресной строке проводника нажмите и введите `cmd`
   - Нажмите **Enter**

3. **Установите зависимости**:
   ```
   pip install -r requirements.txt
   ```
   
   ⏳ Подождите, пока установятся все модули

---

### Шаг 5️⃣: Скопируйте xray.exe

1. **Найдите `xray.exe`** из шага 2
2. **Скопируйте его** в папку проекта (где лежит `xray_proxy_manager.py`)

---

### Шаг 6️⃣: Запустите программу!

1. **Откройте командную строку** в папке проекта
2. **Введите команду**:
   ```
   python xray_proxy_manager.py
   ```
3. **Нажмите Enter** и пользуйтесь! 🎉

---

## 📖 Как использовать?

### 🕹 Управление происходит стрелочками ↑↓ на клавиатуре

**Главное меню:**

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 ⚡ XRAY MANAGER STATUS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 ● RUN | {Port} | {name_sharelink} | {ip} {city}, {country}
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[?] Действие:
   🛠  Управление портами
   🔍 Проверить IP
   📊 Активность
   ⚙️ Настройки
   ⚡ Автозапуск (ВКЛ)
   🚪 Выход
```

---

## 1️⃣ Управление портами

### Добавить новый порт

- Выберите **🛠 Управление портами** → **Добавить порт**
- Введите номер порта (1024-65535)
- Вставьте ваш Share Link (VLESS, VMess, Trojan, Shadowsocks)
- Программа автоматически запустит порт и проверит IP

### Включить / Выключить порт

- Выберите **🛠 Управление портами** → **Вкл/Выкл порт**
- Выберите нужный порт из списка (с индикатором статуса ● / ○)
- Программа включит или выключит его

### Изменить Share Link

- Выберите **🛠 Управление портами** → **Изменить Share Link**
- Выберите порт из списка
- Введите новый Share Link
- Программа перезапустит порт с новой конфигурацией

### Удалить порт

- Выберите **🛠 Управление портами** → **Удалить порт**
- Выберите порт из списка
- Подтвердите удаление через диалог Yes/No
- **Требуется ввод**: введите `y` или `yes` + Enter для подтверждения

---

## 2️⃣ Проверить IP

**Проверить внешний IP через прокси:**
- Выберите **🔍 Проверить IP**
- Программа проверит IP всех активных портов
- Покажет: **IP → Город, Страна → ISP → Timezone**
- IP кэшируется и отображается в главном меню

> 💡 **Совет**: Убедитесь, что установлен `pysocks`:
> ```
> pip install pysocks
> ```

---

## 3️⃣ Активность портов

**Реальный мониторинг активности (10 сек обновление):**
- Выберите **📊 Активность**
- Программа будет показывать в реальном времени:
  - **Активные соединения** по каждому порту
  - **IP адреса** и порты назначения
  - **Состояния соединений** (ESTABLISHED, TIME_WAIT, CLOSE_WAIT)
  - **Трафик на каждый порт** отдельно (KB с указанием MB в скобках)
  - **Общий трафик** всех портов
  - **Время мониторинга**

Пример отображения трафика: `128.5 KB (0.13 MB)`

> 📌 Для полного функционала мониторинга трафика установите `psutil`:
> ```
> pip install psutil
> ```

---

## ⚙️ Настройки

### Режимы работы процессов

Программа поддерживает два режима:

#### 🔹 Unified режим (экономия ресурсов) — **По умолчанию**

- Все порты работают в **одном процессе Xray**
- Экономит оперативную память
- Все порты зависят друг от друга (если упадет процесс — упадут все)
- Подходит для большого количества портов

**Использование**: выберите **⚙️ Настройки** → **Режим процессов** → **Unified**

#### 🔹 Separate режим (независимое управление)

- Каждый порт работает в **отдельном процессе Xray**
- Каждый порт контролируется независимо
- Более высокое потребление памяти
- Лучшая стабильность каждого порта отдельно
- Подходит для критичных прокси

**Использование**: выберите **⚙️ Настройки** → **Режим процессов** → **Separate**

#### 🔄 Миграция между режимами

- При переключении режима программа **автоматически перезапустит** все активные порты
- Конфигурация портов сохранится и восстановится в новом режиме

---

## 🎮 Примеры использования

### Пример 1: Быстрый старт

```bash
# Запустить программу
python xray_proxy_manager.py

# При первом запуске:
# - Выберите "Управление портами" → "Добавить порт"
# - Введите номер порта: 9999
# - Введите Share Link (VLESS): vless://uuid@...
# - Программа запустит порт и покажет IP
```

### Пример 2: Проверка IP через прокси

```bash
# Установите pysocks для работы SOCKS5
pip install pysocks

# В программе выберите: "Проверить IP"
# Увидите реальный IP вашего профиля через прокси
# IP будет кэширован и показан в главном меню
```

### Пример 3: Мониторинг активности

```bash
# Установите psutil для отображения трафика
pip install psutil

# В программе выберите: "Активность"
# Смотрите активные соединения и трафик в реальном времени
```

### Пример 4: Переключение режима

```bash
# Если используете много портов и нужна экономия памяти:
# Выберите "Настройки" → "Режим процессов" → "Unified"

# Если критична независимость каждого порта:
# Выберите "Настройки" → "Режим процессов" → "Separate"
```

### Пример 5: Диагностика проблем

```bash
# Если IP не работает в основной программе
python xray_ip_test.py

# Введите порт и Share Link
# Скрипт покажет ошибку и поможет диагностировать проблему
```

---

## 📝 Форматы Share Link

Программа поддерживает:

- **VLESS**: `vless://uuid@host:port?security=tls&type=tcp#name`
- **VMess**: `vmess://base64encoded...`
- **Trojan**: `trojan://password@host:port#name`
- **Shadowsocks**: `ss://method:password@host:port#name`

---

## 📦 requirements.txt

```
requests
pysocks
psutil
inquirer
```

**Установка:**
```
pip install -r requirements.txt
```

**Описание зависимостей:**
- `requests` — для HTTP запросов к API проверки IP
- `pysocks` — для поддержки SOCKS5 прокси (необходим для проверки IP)
- `psutil` — для детального мониторинга трафика (опционально, улучшает функционал)
- `inquirer` — для интерактивного меню с навигацией стрелками (опционально, есть fallback)

---

## 💾 Конфигурационные файлы

Программа автоматически создает и использует следующие файлы:

- **`configs_proxy_manager.json`** — основной файл конфигурации со всеми портами и их статусами
- **`xray_config_port_{port}.json`** — временные конфиги для каждого порта (удаляются автоматически)

> 💡 **Все конфигурации сохраняются автоматически** при добавлении/удалении/изменении портов

---

## 🐛 Часто встречаемые проблемы

### ❌ "xray.exe не найден"

**Решение**: Скопируйте `xray.exe` из папки Xray Core в папку проекта

### ❌ "Port already in use" (Порт уже занят)

**Решение**: 
1. Используйте другой порт (1024-65535)
2. Или закройте программу/приложение, которое занимает этот порт

### ❌ "Missing dependencies for SOCKS support"

**Решение**:
```bash
pip install pysocks
```

### ❌ "ModuleNotFoundError: No module named 'inquirer'"

**Решение**:
```bash
pip install inquirer
```

Программа имеет fallback режим и будет работать и без inquirer, но интерфейс будет менее удобным.

### ❌ "IP не определяется"

**Решение**:
1. Проверьте Share Link (правильный ли UUID/адрес)
2. Проверьте интернет соединение
3. Убедитесь, что pysocks установлен: `pip install pysocks`
4. Используйте диагностику:
   ```
   python xray_ip_test.py
   ```

### ❌ "Процесс не завершается при выходе"

**Решение**:
1. Программа должна завершиться автоматически через несколько секунд
2. Если этого не произошло, нажмите `Ctrl+C`
3. При выходе все порты будут сохранены и загружены при следующем запуске

---

## 💡 Советы и рекомендации

- 💾 **Конфигурация сохраняется автоматически** в `configs_proxy_manager.json`
- 🔄 **При перезагрузке программы** все портов восстанавливаются автоматически
- 📍 **Первый запуск быстрый** — просто введите порт и share link
- ⚡ **Используйте разные порты** для разных регионов/профилей
- 🌍 **IP проверяется через** api.ip.sb (с fallback на api-ipv4.ip.sb)
- 🎯 **Выбирайте Unified режим** если много портов (экономия памяти)
- 🔧 **Выбирайте Separate режим** если критична стабильность каждого портов

---

## 🔐 Безопасность

- Все конфиги хранятся **локально** в JSON файлах
- **Никаких данных** не отправляется куда-либо, кроме API для проверки IP
- Программа работает **полностью офлайн** после первоначальной настройки
- Защита от случайного удаления портов через подтверждение

---

## 📄 Лицензия

MIT License — используйте свободно! 💗

---

## 🙌 Спасибо за использование!

Если программа помогла вам, оставьте ⭐ **Star** на GitHub! 

---

## 🤖 История разработки

Программа была разработана с целью использования V2Ray возможностей в браузере Chrome в связке с расширением [Proxy SwitchyOmega 3 (ZeroOmega)](https://chromewebstore.google.com/detail/proxy-switchyomega-3-zero/pfnededegaaopdmhkdmcofjmoldfiped).

Однако вы можете использовать локальный прокси как угодно:
- Minecraft клиенты с ProxyMod
- Анти-детект браузеры
- Любые приложения с поддержкой SOCKS5

---

**Версия**: 3.3
**Статус**: ✅ Стабильная и готовая к использованию
**Код оптимизирован**: -39% строк кода (с 1581 до 967 строк)
**Разработано с помощью**: Claude AI (Haiku 4.5)
